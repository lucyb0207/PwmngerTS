generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id           String   @id @default(uuid())
  email        String   @unique
  passwordHash String
  /// JSON array of KDF salt bytes (shared with client vault); required for HKDF auth derivation
  kdfSalt      String?
  twoFactorSecret String? // Null = 2FA disabled
  authenticators Authenticator[]
  vault        Vault?
  refreshTokens RefreshToken[]
  createdAt    DateTime @default(now())
}

model Authenticator {
  id                   String   @id @default(uuid())
  credentialID         String   @unique
  credentialPublicKey  String
  counter              BigInt
  credentialDeviceType String
  credentialBackedUp   Boolean
  transports           String?  // Comma-separated or JSON
  userId               String
  user                 User     @relation(fields: [userId], references: [id])
  createdAt            DateTime @default(now())
}

model RefreshToken {
  id        String   @id @default(uuid())
  token     String   @unique
  userId    String
  user      User     @relation(fields: [userId], references: [id])
  expiresAt DateTime
  createdAt DateTime @default(now())

  @@index([userId])
}

model Vault {
  id          String   @id @default(uuid())
  encrypted   String   // base64 encrypted blob
  updatedAt   DateTime @updatedAt
  user        User     @relation(fields: [userId], references: [id])
  userId      String   @unique
}
